# 项目协作规则

本项目默认继承 `/Users/sunxinxin/Documents/Project/AGENTS.md` 的通用协作规则。工作时先理解现有项目，只做当前目标所需的最小改动，不覆盖用户已有修改，不把局部问题扩大为无关重构。

## 权威入口

- 业务交付、自动门禁、迁移确认和发布：`docs/ai-delivery-flow.md`
- 页面样板、组件、交互和页面验收：`docs/ai-development-rules.md`
- 本地启动、数据库初始化、迁移和默认账号：`docs/数据库.md`
- 数据库结构：`backend/db/init/001_schema.sql`、`backend/db/migrations/`、`docs/数据库表结构.md`、`docs/数据库表结构.xlsx`
- MCP 接入、工具契约、安全和智能体配置：`docs/MCP智能体接入.md`
- 正式环境部署、缓存、目录权限和健康检查：`deploy/README.md`

专项文档负责会持续变化的实现细节；本文件只保留跨模块硬约束。规则冲突时，先以本文件和 `docs/ai-delivery-flow.md` 的安全、权限、数据和发布边界为准，再核对专项文档与当前代码、测试是否一致，不能照搬已经过期的固定数量或页面清单。

## 需求确认与连续开发

用户提出功能、优化或修复后，AI 先核对相关页面、接口、数据结构、权限和既有实现。业务规则、主页面内容、数据关系、权限边界和高风险事项确认清楚后，只确认一次是否进入开发；用户确认后连续完成实现、自动检查、迁移准备和可验收环境，不重复设置设计、计划或分段审批。

用户已经明确“其余细节由 AI 补充”时，低风险细节依据现有架构和组件自行完成。仅在以下情况暂停确认：重大业务歧义、新增权限或外部系统授权、不可逆数据处理、正式环境操作，或者数据库结构尚未获得用户确认。

## 组件和页面样板

业务页面变更必须先遵守 `docs/ai-development-rules.md`，直接从对应页面样板和 `components/admin` 已有能力开始，不先拼一套业务实现再事后对齐。

AI 在判断底座没有某项能力之前，必须按“用户说法与同义词 → 开发规则能力映射 → 组件入口 → 组件工作台示例 → 实际行为和测试”完成检索；没有这条证据链，不得宣称底座不支持。

开发新功能时，开始实现前必须向用户输出简短的“组件支撑情况”。支持结论只使用“全部支持、部分支持、暂不支持、不涉及前端组件”。部分支持或暂不支持时必须给出建议结论，并在“业务内最小处理、扩展现有底座组件、新增底座组件”中明确推荐一种。低频、一次性且业务特有的能力允许业务系统自行开发；只有存在明确复用价值时才扩展或新增底座组件。

通用组件新增或扩展后同步补充组件工作台示例和测试。组件工作台各子页必须独立授权，权限键使用包含查询参数的完整地址，父菜单不得自动放开相邻分类。

## 数据、接口和数据库

除组件工作台、纯视觉示例和明确 mock 场景外，业务页面必须读取真实接口，写操作必须落到数据库或现有持久化来源。前端 API 层负责将后端字段转换为页面 record，组件不内置业务假数据。

数据库物理表统一使用 `pms_` 前缀。新增或修改结构时，必须在用户确认表、字段、索引、关联和数据影响后，一次性同步：

- `backend/db/init/001_schema.sql`
- `backend/db/migrations/` 增量迁移
- 后端 SQL、接口和测试
- `docs/数据库表结构.md`
- `docs/数据库表结构.xlsx`

初始化数据目录可以随版本增长；迁移基线判断不得依赖菜单、档案类型等种子数量固定不变，但必须阻止在已存在真实业务数据的数据库上建立基线。

## MCP 开发规则

新增或调整 MCP 工具前先阅读 `docs/MCP智能体接入.md`。交付必须同时闭环工具目录、输入输出 Schema、Query/Action 分组、公开工具解析与 dispatcher、动态菜单和按钮权限、业务负责人校验、错误契约、审计、测试及接入文档，不能只新增服务函数或只让本地客户端可调用。

Query 只读；Action 必须保持 `preview → 用户确认 → execute`，执行时使用相同业务参数和确认号，并遵守幂等、单次使用、有效期和执行后查询验证。文件操作只接受受信任 URL，不接收任意地址或把凭据、文件正文写入日志。MCP 工具数量和字段以当前 catalog、Schema、权限过滤后的 `tools/list` 及契约测试为准，不在规则中维护容易过期的固定数字。

修改 MCP 后必须确认 3103 后端已加载最新源码，再验证工具发现、成功调用、权限拒绝、参数错误和 Action 完整确认链路。

## 外部接口与自动同步

新外部接口优先接入现有通用接口管理，不为每个来源复制一套配置、列表和执行页面。通用管理只承接接口配置、启停、测试连接、手动同步、自动执行配置和执行历史；来源字段映射留在独立适配器中。

同步实现必须明确首次执行时间、间隔、时区、手动与自动触发来源、超时、重复数据判定、失败重试和映射版本。手动执行不得无意改变下一次自动计划；每次执行都必须留下历史，失败记录不得被后一次成功覆盖。

外部数据新增或更新业务记录时，必须遵守与页面操作相同的数据校验、默认值和权限边界，并记录真实变更历史。无变化时不得生成空历史；映射规则变化时允许按新版本重新处理并在执行明细说明新增、更新、跳过或失败原因。

## 变更历史和状态一致性

新增、编辑、删除、状态变更、批量操作、MCP 操作和外部同步都必须评估变更历史。一次业务动作产生的多字段日志共享 `pms_op_log.operation_id`；详情展示前完成中文字段名、关联对象、枚举状态和日期转译。只有真实变化才记录字段明细，创建、删除等事件可以保留不可展开的操作节点。

同一业务状态的数据库代码、后端校验、前端 `statusTransitions`、列表标签、详情标签、状态变更选项、变更历史和 MCP `*_label` 必须使用同一套业务含义和中文名称。修改任一处时同时检查其他入口，禁止出现“展示叫处理中、操作叫进行中”等漂移。

## Git、版本与正式发布

Git 交付和正式环境发布是两个独立阶段。用户要求“提交 Git、合并、更新 Git”时，默认只处理本次范围内的文件：检查工作区和差异、运行完整门禁、提交并推送 `codex/` 分支、创建或更新 PR、等待远程检查、合并 `master`，最后核对本地 `master` 与 `origin/master` 一致。不得混入 `tmp/`、用户文档或其他无关未提交修改。

产品运行代码、数据库初始化或生产部署配置发生变化时，同步更新 `frontend/src/modules/release-notes/release-notes.json`，版本使用 `vYYYY.MM.DD.N`。版本记录表示准备发布的产品内容，不等于正式环境已经部署。

只有用户明确要求发布正式环境后，才能按照 `deploy/README.md` 执行备份、迁移检查、构建、不可变发布目录切换和线上验证。发布后必须核对后端健康检查、前端公开入口、核心业务流程、Nginx 目录权限、CSP 和缓存衔接；失败时停止继续操作并按可恢复方案回滚，不能把 Git 合并、构建成功或后台进程 PID 当作已发布。

## 完成前验收

所有变更运行 `node scripts/verify-change.mjs`。页面变更还必须使用浏览器实际检查受影响流程；接口、数据库、MCP 和外部同步变更必须验证真实读写或调用结果。执行依赖安装、构建或可能影响本地进程的命令后，重新确认后端 3103 健康检查为 200、前端 3104 可访问，并检查运行进程已加载最新源码。

交付时直接说明变更内容、影响范围、验证证据、migration 状态、Git/发布状态和剩余风险。无法完成的验收项必须明确列出，不能笼统宣称完成。
