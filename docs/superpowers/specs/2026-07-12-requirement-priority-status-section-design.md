# 需求详情优先级位置调整设计

## 目标

将需求详情中的“优先级”从基本信息移动到右侧状态栏，减少详情信息重复并突出业务状态。

## 页面规则

- 右侧状态栏顺序固定为：当前状态、优先级、逾期状态。
- 优先级使用 PMIS 现有 `StatusTag` 展示：低、中、高分别采用弱提示、处理中、危险语义。
- 基本信息中删除“优先级”字段，不重复展示。
- 列表、新增编辑、接口和数据库均不变。

## 验证

- 增加页面语义回归测试，锁定三项状态顺序并确认基本信息不再包含优先级。
- 运行前端测试、组件严格审计、生产构建，并在浏览器打开现有需求详情核对右侧展示。
