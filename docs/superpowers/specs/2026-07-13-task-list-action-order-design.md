# 任务列表操作顺序设计

## 目标

将任务列表单条操作顺序从“编辑、复制、状态变更、删除”调整为“编辑、状态变更、复制、删除”。

## 约束

- 继续使用 `OperationColumnActions` 和现有动作组件。
- 仅调整动作声明顺序，不修改动作逻辑、权限、弹窗、接口或数据库。
- 调整后“编辑”“状态变更”直接显示，“复制”“删除”进入“更多”。

## 验收

- 结构测试锁定四个动作的顺序。
- 完整门禁通过。
- 浏览器中任务列表直接显示“编辑”“状态变更”，“更多”内包含“复制”“删除”。
