# 接入接口管理落地计划

目标：接口管理主入口管理对外接入系统，保留旧同步临时入口，不停止旧任务、不发布生产。

沿用已批准的三张开放接口表和 `/integrations` 菜单权限，不新增表结构。沿用 React 页面样板和 Express 管理路由。

- [x] 管理接口：新增 `/api/open-clients`，复用接口管理菜单权限；白名单返回配置字段，提供新增、编辑、启停、重置凭证、调用历史。凭证只在重置响应出现并禁缓存，变更事务内审计，禁止客户端提交管理操作人。测试验证脱敏、权限、校验、凭证轮换和历史。
- [x] 页面：主列表复用 TemplateListPage；新增编辑复用 TemplateFormPage；详情复用 TemplateDetailPage；历史使用服务端分页，支持结果和来源编号筛选。所有页面沿用列表返回上下文。旧同步独立路径 `/integrations/legacy`，主页面提供临时入口。
- [x] 验收：真实数据库隔离测试、完整 verify-change 门禁、重启后端新鲜度、浏览器新增编辑启停凭证和历史读回；同步接入手册、版本记录及交付说明。测试凭证不外发、不调用生产。

验收结果：前端523通过，后端559通过、3跳过；独立真实数据库接口测试通过。桌面浏览器验证新增、校验、编辑、凭证、启用、真实调用与失败历史、返回上下文及旧同步入口，测试数据已清理。配置编辑字段审计及并发锁定已通过复核。本轮无新增迁移，本地无待执行迁移；尚未提交Git或发布生产。手机窄屏仍受后台底座布局限制，未做专项适配。
