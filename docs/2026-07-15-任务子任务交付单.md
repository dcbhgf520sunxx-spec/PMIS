# 任务子任务交付单

## 变更内容

- 页面/组件：任务列表增加主任务、子任务层级标识；有子任务的主任务默认收起并支持单组展开、收起，无子任务时不显示主标签；列表不展示子任务完成数量；主、子标签统一为 `18×18px` 小正方形、字号 `10px`，子标签使用灰色并相对主任务向右缩进一级，层级区域采用紧凑无底色样式；主任务详情增加子任务表和新增入口；子任务详情展示所属主任务；新增、编辑子任务时锁定并继承主任务的关联类型与关联对象；最后一个子任务完成后弹窗提示处理主任务。
- 接口/权限/菜单：新增 `GET /api/tasks/:id/subtasks`、`POST /api/tasks/:id/subtasks`；沿用任务管理权限与既有状态、操作列、删除确认组件；未新增菜单和权限键。
- 数据库：`pms_task` 新增可空字段 `parent_task_id`，外键引用 `pms_task(id)`，增加父任务查询索引；初始化 SQL、增量 migration、Markdown 和 Excel 结构文档已同步。

## 自动门禁

- 路由菜单权限检查：通过；新增子任务路由沿用任务管理权限。
- 前端组件审查与构建：严格组件审计 0 阻断、0 提醒；API 契约审计 0 阻断；前端 173 项 `.mjs` 测试通过；生产构建通过。
- 后端测试：114 项全部通过；操作历史审计 0 阻断。
- 后端运行态新鲜度：统一门禁确认当前服务已加载最新源码。
- migration 状态：`20260715_add_task_parent.sql` 已执行，当前没有待执行 migration。
- 浏览器冒烟：在 `http://localhost:3104` 实测任务列表、主任务详情、子任务详情、新增子任务表单及状态流转；任务列表额外验证无子任务不显示主标签、有子任务默认展开、主任务后无完成数量、子任务向右缩进一级、收起隐藏子任务、重新展开恢复子任务、灰色子标签和无色缩进；临时验收数据已清理。
- 页面语义：列表和详情继续使用统一操作列、删除确认和状态变更组件；状态操作位于当前状态区。
- 标签色彩：有子任务的主任务使用 `CategoryTag`，子任务使用默认灰色 `AdminTag`；无子任务不显示层级标签。
- 变更历史中文化：父任务字段展示为“所属主任务”，关联值转换为任务名称。
- 列表列行为：保留既有固定列、可调列宽、横向滚动和排序契约；创建人、创建时间位于操作列前。
- 接口运行时契约：子任务读取返回数组契约；子任务创建和状态更新返回对象契约均已声明并通过审计。
- 状态流转契约：主任务存在未完成子任务时禁止完成；主任务已完成后，子任务禁止离开已完成状态；最后一个子任务完成时返回联动提示标记，主任务不自动完成。
- 菜单授权：无新增菜单；完整地址权限和既有一级菜单顺序不变。

## 验收

- 验收环境和地址：本地后端 `http://localhost:3103` 健康检查 200、数据库已连接；前端 `http://localhost:3104` 返回 200。
- 建议验证的业务动作：新建主任务后新增多个子任务；确认子任务继承关联对象；逐个完成子任务；确认最后一个完成后提示；再手工完成主任务。
- 已知风险或未验证项：未执行生产发布；现有历史任务默认均为主任务，不受影响。

## 下一步

- 请完成最终业务验收；确认发布后由 AI 执行部署和线上健康检查。
