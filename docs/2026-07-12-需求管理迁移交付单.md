# 需求管理迁移交付单

## 变更内容

- 页面/组件：新增需求列表、新增编辑、详情、状态变更和变更历史，复用 PMIS 页面样板与公共组件。
- 接口/权限/菜单：新增 `/api/requirements` 完整接口、`/requirements` 菜单权限、前端路由和侧栏入口；项目模块补充只读选项接口供需求表单联动。
- 数据库：新增 `pms_requirement` 空表、索引、菜单增量迁移；不迁移原系统历史数据，migration 已执行。

## 自动门禁

- 路由菜单权限检查：通过。
- 前端组件审查与构建：普通/严格审计均 0 阻断、0 提醒；生产构建通过。
- 后端测试：68 项通过。
- migration 状态：`20260712_add_requirement.sql` 已执行，复查无待执行迁移。
- 浏览器冒烟：需求菜单、空列表、筛选区、新增表单、富文本编辑器和必填校验均通过。
- 页面语义：操作列使用统一文字动作；删除、状态变更、详情状态区和历史均复用公共组件。
- 列表列行为：左右固定列、列宽、横向滚动和服务端排序已接入。
- 接口运行时契约：列表、详情、历史、项目选项和写入返回均声明运行时契约。
- 状态流转契约：四类需求路径、通用实施阶段、暂停恢复、完成/暂停附加字段均由前后端共同校验。
- 菜单授权：完整地址 `/requirements` 已加入菜单和管理员角色授权，一级菜单顺序保持不变。

## 验收

- 验收环境和地址：后端 `http://localhost:3103`，前端 `http://localhost:3104/requirements`。
- 已验证业务动作：临时需求创建、详情查询、状态变更、历史查询、删除完整闭环；删除后有效需求数为 0。
- 已知风险或未验证项：未迁移历史数据；未接入任务、缺陷，符合本次范围。尚未部署生产环境。

## 下一步

- 请在本地完成最终业务验收；确认发布后再执行生产部署和线上健康检查。
