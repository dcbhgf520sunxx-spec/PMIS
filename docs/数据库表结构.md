# 数据库表结构

本文档给技术和 AI 阅读，方便代码审查和 Git diff。业务核对字段时优先查看 `docs/数据库表结构.xlsx`。

## 维护规则

- SQL 初始化源头：`backend/db/init/001_schema.sql`
- 已有数据库升级：`backend/db/migrations/*.sql`
- 技术/AI 文档：`docs/数据库表结构.md`
- 业务核对表：`docs/数据库表结构.xlsx`

新增或修改表结构时，以上四处必须同步。不能只改 SQL，也不能只改 Excel。

## 设计约定

- 数据库：PostgreSQL
- 表名前缀：`pms_`
- 主键：默认使用 `id BIGSERIAL PRIMARY KEY`
- 删除标记：业务主表默认使用 `is_deleted SMALLINT`，`0` 正常，`1` 删除
- 时间字段：统一使用 `TIMESTAMPTZ`，默认 `NOW()`
- 接口返回：后端字段保持数据库命名，前端 API 层转换为页面 record

## 表清单

| 表名 | 中文名 | 说明 |
|---|---|---|
| `pms_user` | 用户表 | 后台用户、登录账号、状态和审计信息 |
| `pms_role` | 角色表 | 角色编码、名称、说明和审计信息 |
| `pms_user_role` | 用户角色关联表 | 用户与角色授权关系 |
| `pms_menu` | 菜单表 | 后台菜单、路由、权限点和显示顺序 |
| `pms_role_menu` | 角色菜单关联表 | 角色与菜单权限授权关系 |
| `pms_work_order` | 运维工单表 | 工单主数据、状态、跟进人和处理时间 |
| `pms_business_attachment` | 通用业务附件表 | 需求、项目、任务、BUG、运维工单的 OSS 附件元数据 |
| `pms_product` | 产品表 | 产品名称、负责人、状态和审计信息 |
| `pms_product_maintenance_contract` | 产品运维合同表 | 产品历次运维合同、续签关系、服务周期和终止信息 |
| `pms_product_maintenance_contract_attachment` | 产品运维合同附件表 | 运维合同附件元数据及 OSS 上传响应 |
| `pms_project` | 项目表 | 项目归属、负责人、优先级、状态、日期、进展和风险 |
| `pms_project_member` | 项目成员关联表 | 项目与参与用户的多对多关系 |
| `pms_project_plan_template` | 阶段主计划模板表 | 模板编码、名称、启停状态和顺序 |
| `pms_project_plan_template_stage` | 阶段主计划模板阶段表 | 模板内的阶段定义和顺序 |
| `pms_project_plan_template_item` | 阶段主计划模板事项表 | 模板阶段内的关键事项和交付要求 |
| `pms_project_plan_stage` | 项目计划阶段表 | 项目阶段、说明和显示顺序 |
| `pms_project_plan_item` | 项目计划关键事项表 | 负责人、状态、计划日期和交付要求 |
| `pms_project_plan_item_collaborator` | 关键事项协作人表 | 关键事项与协作人的多对多关系 |
| `pms_project_plan_adjustment` | 关键事项计划调整表 | 计划完成时间调整历史 |
| `pms_project_plan_delivery_file` | 关键事项交付文件表 | 关键交付文件及版本历史 |
| `pms_project_contract` | 项目合同表 | 项目供应商合同基本信息，一项目一合同 |
| `pms_project_contract_attachment` | 项目合同附件表 | 合同附件元数据及 OSS 上传响应 |
| `pms_project_payment_stage` | 项目付款阶段表 | 合同的阶段付款计划 |
| `pms_project_payment_record` | 项目付款记录表 | 付款阶段下的实际付款明细 |
| `pms_requirement` | 需求表 | 需求路径、状态、负责人、计划日期和完成信息 |
| `pms_task` | 任务表 | 任务关联对象、类型、优先级、状态和处理时间 |
| `pms_task_owner` | 任务负责人关联表 | 任务与平级负责人的多对多关系 |
| `pms_follow_up_record` | 跟进记录表 | 项目、需求和任务共用的人工跟进记录 |
| `pms_bug` | BUG 表 | BUG 关联对象、类型、严重程度、状态、指派人和解决方案 |
| `pms_archive_type` | 档案类型表 | 基础档案分类 |
| `pms_archive` | 基础档案表 | 基础档案明细 |
| `pms_op_log` | 操作日志表 | 用户操作审计日志 |
| `pms_access_log` | 访问日志表 | 登录访问记录、登录结果、会话时长和客户端信息 |
| `pms_user_preference` | 用户偏好设置表 | 默认进入页面、列表默认条数和外观兼容配置 |
| `pms_message` | 消息表 | 通知和系统消息，记录接收人、已读状态和跳转路径 |
| `pms_scheduled_task_execution` | 定时任务执行表 | 通用定时任务幂等、执行状态、重试和结果记录 |

## pms_user 用户表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `employee_no` | `VARCHAR(50)` | 必填，唯一 | 工号，登录和列表标识 |
| `real_name` | `VARCHAR(50)` | 必填 | 用户姓名 |
| `phone` | `VARCHAR(20)` | 必填，唯一 | 手机号 |
| `avatar_url` | `VARCHAR(300)` | 可空 | 头像地址 |
| `password` | `VARCHAR(255)` | 必填 | bcrypt 加密密码 |
| `status` | `SMALLINT` | 默认 `1` | 1 启用，0 停用 |
| `first_login` | `SMALLINT` | 默认 `1` | 1 首次登录，0 非首次 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_user_status_deleted(status, is_deleted)`

## pms_role 角色表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `code` | `VARCHAR(50)` | 必填，未删除数据唯一 | 角色编码；软删除后可复用 |
| `name` | `VARCHAR(50)` | 必填 | 角色名称 |
| `description` | `VARCHAR(200)` | 可空 | 角色说明 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_role_deleted(is_deleted)`、`ux_pms_role_code_active(code) WHERE is_deleted = 0`

## pms_user_role 用户角色关联表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `user_id` | `BIGINT` | 必填，引用 `pms_user.id` | 用户 |
| `role_id` | `BIGINT` | 必填，引用 `pms_role.id` | 角色 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |

唯一约束：`UNIQUE(user_id, role_id)`

## pms_menu 菜单表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `parent_id` | `BIGINT` | 默认 `0` | 父级菜单，0 表示一级菜单 |
| `name` | `VARCHAR(50)` | 必填 | 菜单名称 |
| `code` | `VARCHAR(50)` | 必填，唯一 | 菜单编码；组件工作台子菜单使用 `design_system_*` |
| `type` | `SMALLINT` | 默认 `1` | 1 目录，2 页面，3 按钮权限 |
| `path` | `VARCHAR(200)` | 可空 | 完整前端菜单权限路径，可包含查询参数 |
| `icon` | `VARCHAR(50)` | 可空 | 图标 |
| `sort_order` | `INTEGER` | 默认 `0` | 排序号 |
| `status` | `SMALLINT` | 默认 `1` | 状态 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_menu_path(path)`

一级菜单顺序固定为：首页 `5`、产品管理 `6`、项目管理 `7`、需求管理 `8`、任务管理 `9`、BUG管理 `10`、运维工单 `11`、基础设置 `20`、用户权限 `30`、组件工作台 `40`。

优先级调整使用独立按钮权限，不新增业务表：`requirement_priority_adjust`（需求优先级调整）、`project_priority_adjust`（项目优先级调整）、`task_priority_adjust`（任务优先级调整）。三个权限点默认仅授予管理员角色，其他角色按需授权。

组件工作台以 `design_system` 为父目录，以下页面作为可独立授权的二级菜单：

| code | path |
|---|---|
| `design_system_overview` | `/system/design-system?category=overview` |
| `design_system_samples` | `/samples/work-order` |
| `design_system_foundation` | `/system/design-system?category=foundation` |
| `design_system_layout` | `/system/design-system?category=layout` |
| `design_system_base` | `/system/design-system?category=base` |
| `design_system_input` | `/system/design-system?category=input` |
| `design_system_feedback` | `/system/design-system?category=feedback` |
| `design_system_display` | `/system/design-system?category=display` |

## pms_role_menu 角色菜单关联表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `role_id` | `BIGINT` | 必填，引用 `pms_role.id` | 角色 |
| `menu_id` | `BIGINT` | 必填，引用 `pms_menu.id` | 菜单 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |

唯一约束：`UNIQUE(role_id, menu_id)`

## pms_work_order 运维工单表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `product_id` | `BIGINT` | 必填，引用 `pms_product.id` | 所属产品，新增和变更时必须为启用状态 |
| `problem_type` | `BIGINT` | 必填，引用 `pms_archive.id` | 问题类型档案，新增和变更时必须为启用的 `PT` 类型 |
| `problem_desc` | `TEXT` | 必填 | 问题描述 |
| `result_desc` | `TEXT` | 可空 | 处置结果 |
| `activation_reason` | `TEXT` | 被激活时必填，最多 100 字 | 本轮激活原因；进入待处理或处理中时清空，再次激活时更新 |
| `follower_id` | `BIGINT` | 必填，引用 `pms_user.id` | 跟进人 |
| `urgency` | `SMALLINT` | 默认 `1` | 紧急程度 |
| `status` | `SMALLINT` | 默认 `0` | 工单状态：`0` 待处理、`1` 处理中、`2` 已解决、`4` 已暂停、`5` 被激活；待处理可直接解决，已解决可暂停或激活，被激活后只能重新解决 |
| `is_overdue` | `SMALLINT` | 默认 `0` | 是否逾期 |
| `expected_resolve_date` | `TIMESTAMPTZ` | 可空 | 期望解决时间 |
| `resolve_date` | `TIMESTAMPTZ` | 可空 | 解决时间 |
| `close_date` | `TIMESTAMPTZ` | 可空 | 历史关闭时间；取消已关闭状态后只保留旧数据，不再新增或展示 |
| `suspend_date` | `TIMESTAMPTZ` | 可空 | 暂停时间；进入已暂停时记录，离开已暂停时清空 |
| `submitter_name` | `VARCHAR(50)` | 必填 | 提交人 |
| `submitter_dept` | `VARCHAR(100)` | 必填 | 提交部门 |
| `submit_time` | `TIMESTAMPTZ` | 必填 | 提交时间 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_work_order_status(status, is_deleted)`、`idx_work_order_follower(follower_id)`。问题描述不设唯一索引，允许不同工单使用相同描述。

待处理和处理中均可变更为已解决，变更时必须填写实际修复时间和处置结果。激活工单时必须填写 1 至 100 字的激活原因，原预计完成时间保持不变。激活后保留上次实际修复时间、处置结果和历史关闭时间，并按原预计完成时间重新判断逾期；激活原因在已解决和已暂停状态继续保留，进入待处理或处理中时清空。再次解决时使用新的实际修复时间和处置结果覆盖当前值。历史状态值 `3` 继续在操作记录中显示为“已关闭”，现有工单数据统一归并为已解决。

## pms_task 任务表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 任务编号 |
| `name` | `VARCHAR(200)` | 必填，正常数据中唯一 | 任务名称 |
| `description` | `TEXT` | 可空 | 任务描述 |
| `parent_task_id` | `BIGINT` | 可空，引用 `pms_task.id` | 所属主任务；为空表示主任务 |
| `source_type` | `SMALLINT` | 1 项目，2 需求 | 关联类型 |
| `project_id` | `BIGINT` | 可空，引用 `pms_project.id` | 关联项目 |
| `requirement_id` | `BIGINT` | 可空，引用 `pms_requirement.id` | 关联需求 |
| `task_type` | `BIGINT` | 必填，引用 `pms_archive.id` | 任务类型 |
| `priority` | `SMALLINT` | 必填，默认 `0`，仅 `0/1/2` | 优先级：0 低、1 中、2 高 |
| `status` | `SMALLINT` | 默认 0 | 0 未启动，1 进行中，2 已完成，3 暂停 |
| `is_overdue` | `SMALLINT` | 默认 0 | 是否逾期 |
| `start_date` | `DATE` | 可空 | 启动时间 |
| `expected_end_date` | `DATE` | 可空 | 预计完成时间 |
| `actual_end_date` | `DATE` | 可空 | 实际完成时间 |
| `suspend_date` | `DATE` | 可空 | 暂停时间 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 0 | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

项目和需求只能二选一。任务最多两层，子任务通过 `parent_task_id` 关联主任务，层级限制由后端校验。每个任务至少关联一名平级负责人，负责人关系存放在 `pms_task_owner`。索引：`uk_task_name_active`、`idx_task_project`、`idx_task_requirement`、`idx_task_parent`、`idx_task_type_status`、`idx_task_expected_end`。

## pms_task_owner 任务负责人关联表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `task_id` | `BIGINT` | 主键组成部分，引用 `pms_task.id`，任务删除时级联删除 | 任务 |
| `user_id` | `BIGINT` | 主键组成部分，引用 `pms_user.id` | 平级负责人 |
| `sort_order` | `INTEGER` | 默认 `0` | 负责人展示顺序 |

联合主键：`task_id, user_id`。索引：`idx_task_owner_user(user_id, task_id)`，用于“我的任务”和负责人筛选。

## pms_follow_up_record 跟进记录表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 跟进记录编号 |
| `project_id` | `BIGINT` | 可空，引用 `pms_project.id`，物理删除时级联删除 | 关联项目 |
| `requirement_id` | `BIGINT` | 可空，引用 `pms_requirement.id`，物理删除时级联删除 | 关联需求 |
| `task_id` | `BIGINT` | 可空，引用 `pms_task.id`，物理删除时级联删除 | 关联任务 |
| `content` | `TEXT` | 必填，去除首尾空格后不超过 200 字 | 人工填写的跟进内容 |
| `creator_id` | `BIGINT` | 可空，引用 `pms_user.id` | 跟进人，新增时取当前登录用户 |
| `updater_id` | `BIGINT` | 可空，引用 `pms_user.id` | 最后修改或删除记录的登录用户 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 跟进记录提交时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 最后修改时间 |

`project_id`、`requirement_id`、`task_id` 必须且只能有一个非空。跟进记录不承接状态、负责人、日期等业务字段变更；这些变化继续写入对应业务对象的变更历史。新增、修改和删除跟进记录写入操作日志的独立“跟进记录”模块。索引：`idx_follow_up_project`、`idx_follow_up_requirement`、`idx_follow_up_task`。

## pms_bug BUG 表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | BUG 编号 |
| `source_type` | `SMALLINT` | 必填；1 项目，2 需求 | 关联类型 |
| `project_id` | `BIGINT` | 可空，引用 `pms_project.id` | 关联项目 |
| `requirement_id` | `BIGINT` | 可空，引用 `pms_requirement.id` | 关联需求 |
| `title` | `VARCHAR(200)` | 必填，正常数据中唯一 | Bug 标题 |
| `description` | `TEXT` | 可空 | Bug 描述（富文本） |
| `bug_type_id` | `BIGINT` | 必填，引用 `pms_archive.id` | Bug 类型，档案类型为“Bug类型” |
| `severity` | `SMALLINT` | 必填 | 严重程度：1 低，2 中，3 高，4 致命 |
| `status` | `SMALLINT` | 默认 `0` | 0 新建，1 已修复，2 已关闭，3 被激活 |
| `assignee_id` | `BIGINT` | 必填，引用 `pms_user.id` | 指派给 |
| `resolution_id` | `BIGINT` | 可空，引用 `pms_archive.id` | 解决方案；被激活时保留，再次修复时更新 |
| `resolved_date` | `DATE` | 可空 | 修复时间；被激活时保留，再次修复时更新 |
| `closed_date` | `DATE` | 可空 | 关闭时间 |
| `activation_reason` | `TEXT` | 被激活时必填，最多 100 字 | 最近一次激活原因；后续修复或关闭时继续保留，再次激活时更新 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

关联项目和关联需求必须按关联类型二选一；“已修复”“已关闭”和“被激活”状态可保留解决方案与修复时间，被激活后再次修复时使用新值覆盖；只有“已关闭”状态保留关闭时间；“被激活”状态必须填写激活原因，后续修复或关闭后继续保留最近一次激活原因。索引：`uk_bug_title_active`、`idx_bug_project`、`idx_bug_requirement`、`idx_bug_assignee_status`、`idx_bug_type_status`、`idx_bug_severity_status`、`idx_bug_created_at`。

## pms_product 产品表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `name` | `VARCHAR(100)` | 必填，正常数据中唯一 | 产品名称 |
| `description` | `TEXT` | 可空 | 产品描述 |
| `owner_id` | `BIGINT` | 必填，引用 `pms_user.id` | 产品负责人 |
| `status` | `SMALLINT` | 默认 `1` | 1 启用，0 停用 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_product_name_active(name) WHERE is_deleted = 0`、`idx_product_owner_status(owner_id, status, is_deleted)`

## pms_product_maintenance_contract 产品运维合同表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 合同 ID |
| `product_id` | `BIGINT` | 必填，引用 `pms_product.id` | 所属产品 |
| `previous_contract_id` | `BIGINT` | 可空，自关联本表 | 上一份运维合同；首份合同为空 |
| `contract_code` | `VARCHAR(100)` | 必填，有效数据唯一 | 合同编号 |
| `contract_name` | `VARCHAR(200)` | 必填 | 合同名称 |
| `supplier_id` | `BIGINT` | 必填，引用 `pms_archive.id` | 供应商档案 |
| `signed_date` | `DATE` | 必填 | 签订日期 |
| `service_start_date` | `DATE` | 必填 | 服务开始日期 |
| `service_end_date` | `DATE` | 必填，不早于开始日期 | 服务结束日期 |
| `contract_amount` | `NUMERIC(18,2)` | 必填，大于 0 | 合同金额（元） |
| `termination_date` | `DATE` | 可空 | 提前终止日期，必须位于服务周期内 |
| `termination_reason` | `VARCHAR(500)` | 可空，与终止日期同时填写 | 提前终止原因 |
| `remark` | `TEXT` | 可空 | 备注 |
| `creator_id` / `updater_id` | `BIGINT` | 引用用户 | 创建人和更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

一个产品只允许一条合同续签链：首份有效合同唯一，同一上一份合同只能关联一份有效续签合同。前后合同服务日期不能重叠，允许存在空档；上一份合同提前终止时，以终止日期作为续签日期边界。状态不落库，由终止信息、是否存在续签合同及当前日期推导为待生效、生效中、已到期、已续签或已终止。

固定提醒规则：到期前 30、15、7、3、2、1 天及到期当天提醒；到期后每 7 天提醒一次；续签、终止或删除后停止。接收人为产品负责人，消息写入 `pms_message`。

索引：合同编号有效数据唯一、产品首份合同有效数据唯一、上一份合同有效数据唯一；按产品、供应商和服务结束日期建立查询索引。

## pms_product_maintenance_contract_attachment 产品运维合同附件表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 附件 ID |
| `contract_id` | `BIGINT` | 必填，引用运维合同 | 所属合同 |
| `original_name` | `VARCHAR(255)` | 必填 | 原始文件名 |
| `storage_name` | `VARCHAR(255)` | 必填，唯一 | OSS 文件路径或兼容存储名 |
| `oss_response` | `JSONB` | 可空 | OSS 上传响应 |
| `mime_type` | `VARCHAR(150)` | 必填 | 文件类型 |
| `file_size` | `BIGINT` | 1 至 20MB | 文件大小（字节） |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `creator_id` / `updater_id` | `BIGINT` | 引用用户 | 创建人和更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

索引：`storage_name` 唯一；按合同和显示顺序建立有效附件索引。

## pms_project 项目表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `name` | `VARCHAR(200)` | 必填，正常数据中唯一 | 项目名称 |
| `description` | `TEXT` | 可空 | 项目描述 |
| `product_id` | `BIGINT` | 必填，引用 `pms_product.id` | 所属产品 |
| `requirement_id` | `BIGINT` | 历史数据可空，引用 `pms_requirement.id`；有效项目唯一 | 所属需求；新建和编辑项目时必填 |
| `owner_id` | `BIGINT` | 必填，引用 `pms_user.id` | 项目负责人 |
| `priority` | `SMALLINT` | 必填，默认 `0`，仅 `0/1/2` | 优先级：0 低、1 中、2 高 |
| `status` | `SMALLINT` | 默认 `0` | 0 未启动，1 进行中，2 已完成，3 暂停 |
| `is_overdue` | `SMALLINT` | 默认 `0` | 是否逾期 |
| `start_date` | `DATE` | 可空 | 启动日期 |
| `expected_end_date` | `DATE` | 必填 | 预计完成日期 |
| `actual_end_date` | `DATE` | 可空 | 实际完成日期 |
| `suspend_date` | `DATE` | 可空 | 暂停日期 |
| `progress_text` | `TEXT` | 可空 | 进度记录 |
| `risk_text` | `TEXT` | 可空 | 风险记录 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_project_name_active(name) WHERE is_deleted = 0`、`ux_project_requirement_active(requirement_id) WHERE is_deleted = 0 AND requirement_id IS NOT NULL`、`idx_project_product_status(product_id, status, is_deleted)`、`idx_project_owner_status(owner_id, status, is_deleted)`、`idx_project_expected_end(expected_end_date, is_deleted)`

## pms_project_member 项目成员关联表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `project_id` | `BIGINT` | 必填，引用 `pms_project.id`，级联删除 | 项目 |
| `user_id` | `BIGINT` | 必填，引用 `pms_user.id` | 项目成员 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 加入时间 |

唯一约束：`UNIQUE(project_id, user_id)`；索引：`idx_project_member_user(user_id, project_id)`

## pms_project_plan_template 阶段主计划模板表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 模板 ID |
| `code` | `VARCHAR(100)` | 必填，全局唯一 | 模板编码 |
| `name` | `VARCHAR(200)` | 必填 | 模板名称 |
| `description` | `TEXT` | 可空 | 模板说明 |
| `status` | `SMALLINT` | 默认 `1`，仅 `0/1` | 停用、启用 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

唯一约束：`uk_project_plan_template_code(code)`；索引：`idx_project_plan_template_enabled(status, sort_order, id)`。

## pms_project_plan_template_stage 阶段主计划模板阶段表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 模板阶段 ID |
| `template_id` | `BIGINT` | 必填，引用模板并级联删除 | 所属模板 |
| `name` | `VARCHAR(100)` | 必填，同一模板唯一 | 阶段名称 |
| `description` | `TEXT` | 可空 | 阶段说明 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

唯一约束：`uk_project_plan_template_stage_name(template_id, name)`；索引：`idx_project_plan_template_stage_sort(template_id, sort_order, id)`。

## pms_project_plan_template_item 阶段主计划模板事项表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 模板事项 ID |
| `template_stage_id` | `BIGINT` | 必填，引用模板阶段并级联删除 | 所属模板阶段 |
| `name` | `VARCHAR(200)` | 必填，同一模板阶段唯一 | 关键事项名称 |
| `requires_delivery_file` | `SMALLINT` | 默认 `0`，仅 `0/1` | 是否要求关键交付文件 |
| `delivery_requirement` | `TEXT` | 要求交付文件时必填 | 交付文件要求 |
| `remark` | `TEXT` | 可空 | 默认备注 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

唯一约束：`uk_project_plan_template_item_name(template_stage_id, name)`；索引：`idx_project_plan_template_item_sort(template_stage_id, sort_order, id)`。

## pms_project_plan_stage 项目计划阶段表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 阶段 ID |
| `project_id` | `BIGINT` | 必填，引用 `pms_project.id` | 所属项目 |
| `name` | `VARCHAR(100)` | 必填，同一项目有效数据唯一 | 阶段名称 |
| `description` | `TEXT` | 可空 | 阶段说明 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `creator_id` / `updater_id` | `BIGINT` | 引用用户 | 创建人和更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

索引：`uk_project_plan_stage_name_active(project_id, name) WHERE is_deleted = 0`、`idx_project_plan_stage_project_active(project_id, sort_order, id) WHERE is_deleted = 0`。

## pms_project_plan_item 项目计划关键事项表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 关键事项 ID |
| `stage_id` | `BIGINT` | 必填，引用 `pms_project_plan_stage.id` | 所属阶段 |
| `name` | `VARCHAR(200)` | 必填，同一阶段有效数据唯一 | 关键事项名称 |
| `owner_id` | `BIGINT` | 必填，引用用户 | 主负责人 |
| `status` | `SMALLINT` | 默认 `0`，0-3 | 未开始、进行中、已完成、暂停 |
| `previous_status` | `SMALLINT` | 可空，仅 `0` 或 `1` | 暂停前状态 |
| `pause_reason` | `VARCHAR(200)` | 暂停状态必填 | 当前暂停原因；离开暂停状态后清空，历史原因保留在操作日志 |
| `original_due_date` | `DATE` | 必填 | 原计划完成时间 |
| `current_due_date` | `DATE` | 必填 | 当前有效计划完成时间 |
| `actual_end_date` | `DATE` | 可空 | 实际完成时间 |
| `requires_delivery_file` | `SMALLINT` | 默认 `0` | 是否要求关键交付文件 |
| `delivery_requirement` | `TEXT` | 按文件要求条件必填 | 关键交付文件要求 |
| `remark` | `TEXT` | 可空 | 备注 |
| `sort_order` | `INTEGER` | 默认 `0` | 阶段内显示顺序 |
| `creator_id` / `updater_id` | `BIGINT` | 引用用户 | 创建人和更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

索引：`uk_project_plan_item_name_active(stage_id, name) WHERE is_deleted = 0`、`idx_project_plan_item_stage_active`、`idx_project_plan_item_owner_status`。

## pms_project_plan_item_collaborator 关键事项协作人表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `plan_item_id` | `BIGINT` | 联合主键，引用关键事项，级联删除 | 关键事项 |
| `user_id` | `BIGINT` | 联合主键，引用用户 | 协作人 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |

## pms_project_plan_adjustment 关键事项计划调整表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 调整记录 ID |
| `plan_item_id` | `BIGINT` | 必填，引用关键事项 | 关键事项 |
| `old_due_date` | `DATE` | 必填 | 调整前日期 |
| `new_due_date` | `DATE` | 必填 | 调整后日期 |
| `reason` | `TEXT` | 必填且非空 | 调整原因 |
| `operator_id` | `BIGINT` | 引用用户 | 操作人 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 调整时间 |

索引：`idx_project_plan_adjustment_item(plan_item_id, created_at DESC)`。

## pms_project_plan_delivery_file 关键事项交付文件表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 文件记录 ID |
| `plan_item_id` | `BIGINT` | 必填，引用关键事项 | 关键事项 |
| `original_name` | `VARCHAR(255)` | 必填 | 原文件名 |
| `storage_key` | `VARCHAR(255)` | 必填，唯一 | OSS 文件路径；迁移前历史数据为本地随机存储键 |
| `oss_response` | `JSONB` | 可空 | OSS 上传接口返回的完整 JSON；迁移前历史本地文件为空 |
| `mime_type` | `VARCHAR(150)` | 必填 | MIME 类型 |
| `size_bytes` | `BIGINT` | 大于 0 且不超过 20971520 | 文件大小 |
| `version_no` | `INTEGER` | 默认 `1` | 版本号 |
| `replaces_file_id` | `BIGINT` | 可空，引用本表 | 被替换版本 |
| `is_current` | `SMALLINT` | 默认 `1` | 是否当前版本 |
| `is_void` | `SMALLINT` | 默认 `0` | 是否作废 |
| `change_note` | `TEXT` | 可空 | 补充、替换或作废说明 |
| `uploader_id` | `BIGINT` | 引用用户 | 上传人 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 上传时间 |

索引：`uk_project_plan_delivery_storage_key(storage_key)`、`idx_project_plan_delivery_item_current(plan_item_id, created_at DESC) WHERE is_current = 1 AND is_void = 0`。新上传文件存放在 OSS 的 `pmis` bucket，完整上传响应保存在 `oss_response`，并通过项目权限接口代理下载；历史本地文件由统一迁移脚本转存 OSS。

## pms_project_contract 项目合同表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 合同 ID |
| `project_id` | `BIGINT` | 必填，引用 `pms_project.id`，未删除数据唯一 | 所属项目 |
| `contract_code` | `VARCHAR(100)` | 必填，未删除数据唯一 | 合同编码 |
| `contract_name` | `VARCHAR(200)` | 必填 | 合同名称 |
| `supplier_id` | `BIGINT` | 必填，引用 `pms_archive.id`，限制删除 | 供应商档案 |
| `signed_date` | `DATE` | 必填 | 签订时间 |
| `contract_amount` | `NUMERIC(18,2)` | 必填，大于 0 | 合同金额 |
| `remark` | `TEXT` | 可空 | 备注 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_project_contract_project_active(project_id) WHERE is_deleted = 0`、`uk_project_contract_code_active(contract_code) WHERE is_deleted = 0`、`idx_project_contract_supplier_active(supplier_id) WHERE is_deleted = 0`

已付金额和未付金额不在本表重复存储，由有效付款记录实时汇总。

## pms_project_contract_attachment 项目合同附件表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 附件 ID |
| `contract_id` | `BIGINT` | 必填，引用 `pms_project_contract.id`，限制删除 | 所属合同 |
| `original_name` | `VARCHAR(255)` | 必填 | 上传时原文件名 |
| `storage_name` | `VARCHAR(255)` | 必填，唯一 | OSS 文件路径；历史数据为服务端随机存储名 |
| `oss_response` | `JSONB` | 可空 | OSS 上传接口返回的完整 JSON；历史本地附件为空 |
| `mime_type` | `VARCHAR(150)` | 必填 | 文件 MIME 类型 |
| `file_size` | `BIGINT` | 必填，大于 0 且不超过 20971520 | 文件大小，单位字节 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 上传人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 上传时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_project_contract_attachment_storage_name(storage_name)`、`idx_project_contract_attachment_contract_active(contract_id, sort_order, id) WHERE is_deleted = 0`

新上传附件存放在 OSS 的 `pmis` bucket，完整上传响应保存在 `oss_response`，并通过项目权限接口代理预览或下载；历史附件继续从后端私有目录读取。删除 OSS 附件时只软删除本表关联，不物理删除 OSS 文件。

## pms_business_attachment 通用业务附件表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 附件 ID |
| `business_type` | `VARCHAR(30)` | 必填，限定为 `requirement`、`project`、`task`、`bug`、`work_order` | 业务类型 |
| `business_id` | `BIGINT` | 必填 | 对应业务记录 ID；多态关联，不设置物理外键 |
| `original_name` | `VARCHAR(255)` | 必填 | 上传时原文件名 |
| `mime_type` | `VARCHAR(150)` | 必填 | 文件 MIME 类型 |
| `file_size` | `BIGINT` | 必填，大于 0 且不超过 20971520 | 文件大小，单位字节 |
| `storage_key` | `VARCHAR(255)` | 必填，唯一 | OSS 文件路径 |
| `oss_response` | `JSONB` | 必填 | OSS 上传接口返回的完整 JSON |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 上传人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 上传时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_business_attachment_storage_key(storage_key)`、`idx_business_attachment_active(business_type, business_id, sort_order, id) WHERE is_deleted = 0`。

每条业务记录最多 10 个有效附件，单文件不超过 20 MB。附件统一保存到 OSS；删除业务记录或附件时仅软删除元数据，不物理删除 OSS 文件。MCP 只返回短时下载 URL，不返回原始文件内容或 Base64。

## pms_project_payment_stage 项目付款阶段表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 付款阶段 ID |
| `contract_id` | `BIGINT` | 必填，引用 `pms_project_contract.id` | 所属合同 |
| `stage_name` | `VARCHAR(100)` | 必填，同一合同内未删除数据唯一 | 阶段 |
| `planned_amount` | `NUMERIC(18,2)` | 必填，大于 0 | 计划金额 |
| `sort_order` | `INTEGER` | 默认 `0` | 显示顺序 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 创建人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`uk_project_payment_stage_name_active(contract_id, stage_name) WHERE is_deleted = 0`、`idx_project_payment_stage_contract(contract_id, sort_order, id) WHERE is_deleted = 0`

已付金额、待付金额和付款状态不在本表重复存储，由有效付款记录实时汇总。

## pms_project_payment_record 项目付款记录表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 付款记录 ID |
| `stage_id` | `BIGINT` | 必填，引用 `pms_project_payment_stage.id` | 所属付款阶段 |
| `payment_amount` | `NUMERIC(18,2)` | 必填，大于 0 | 本次付款金额 |
| `payment_month` | `DATE` | 必填，固定为月份第一天 | 付款时间，页面按月展示 |
| `handler_id` | `BIGINT` | 必填，引用 `pms_user.id` | 经办人 |
| `remark` | `TEXT` | 可空 | 备注 |
| `creator_id` | `BIGINT` | 引用 `pms_user.id` | 登记人 |
| `updater_id` | `BIGINT` | 引用 `pms_user.id` | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 登记时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_project_payment_record_stage(stage_id, payment_month, id) WHERE is_deleted = 0`

## pms_requirement 需求表

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 需求 ID |
| `title` | `VARCHAR(200)` | 必填，未删除数据唯一 | 需求标题 |
| `description` | `TEXT` | 可空 | 富文本描述 |
| `requirement_type` | `SMALLINT` | 必填，1-4 | 需求路径 |
| `product_id` | `BIGINT` | 必填，引用产品 | 所属产品 |
| `owner_id` | `BIGINT` | 必填，引用用户 | 负责人 |
| `priority` | `SMALLINT` | 必填，默认 `0`，仅 `0/1/2` | 优先级：0 低、1 中、2 高 |
| `status` | `SMALLINT` | 必填 | 当前状态 |
| `is_overdue` | `SMALLINT` | 可空 | 逾期状态 |
| `submitter_name` | `VARCHAR(50)` | 必填 | 提出人 |
| `submitter_dept` | `VARCHAR(100)` | 可空 | 提出组织 |
| `submit_date` | `DATE` | 必填 | 提出日期 |
| `start_date` | `DATE` | 可空 | 启动日期 |
| `expected_end_date` | `DATE` | 可空 | 预计完成日期 |
| `actual_end_date` | `DATE` | 可空 | 实际完成日期 |
| `pause_date` | `DATE` | 可空 | 暂停日期 |
| `completion_status` | `TEXT` | 可空 | 完成情况 |
| `creator_id` / `updater_id` | `BIGINT` | 引用用户 | 创建人和更新人 |
| `is_deleted` | `SMALLINT` | 默认 0 | 软删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 自动记录 | 创建和更新时间 |
## pms_archive_type 档案类型表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `code` | `VARCHAR(50)` | 必填，未删除数据唯一 | 类型编码；软删除后可复用 |
| `code_prefix` | `VARCHAR(20)` | 必填，未删除数据唯一 | 编码前缀；软删除后可复用 |
| `name` | `VARCHAR(100)` | 必填 | 类型名称 |
| `status` | `SMALLINT` | 默认 `1` | 状态 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`ux_pms_archive_type_code_active(code) WHERE is_deleted = 0`、`ux_pms_archive_type_prefix_active(code_prefix) WHERE is_deleted = 0`

## pms_archive 基础档案表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `code` | `VARCHAR(100)` | 必填，唯一 | 档案编码 |
| `name` | `VARCHAR(100)` | 必填 | 档案名称 |
| `archive_type_id` | `BIGINT` | 必填，引用 `pms_archive_type.id` | 档案类型 |
| `sort_order` | `INTEGER` | 默认 `0` | 排序号 |
| `status` | `SMALLINT` | 默认 `1` | 状态 |
| `creator_id` | `BIGINT` | 可空 | 创建人 |
| `updater_id` | `BIGINT` | 可空 | 更新人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_archive_type(archive_type_id, is_deleted)`

## pms_op_log 操作日志表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `user_id` | `BIGINT` | 引用 `pms_user.id` | 操作用户 |
| `action` | `VARCHAR(50)` | 必填 | 操作类型 |
| `module` | `VARCHAR(50)` | 必填 | 模块 |
| `target_id` | `BIGINT` | 可空 | 操作对象 ID |
| `target_name` | `VARCHAR(200)` | 可空 | 操作对象名称 |
| `operation_id` | `UUID` | 可空 | 同一次业务操作标识，用于聚合多字段变更；历史数据为空时按单条展示 |
| `field_name` | `VARCHAR(100)` | 可空 | 字段名 |
| `old_value` | `TEXT` | 可空 | 旧值 |
| `new_value` | `TEXT` | 可空 | 新值 |
| `ip` | `VARCHAR(50)` | 可空 | IP 地址 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |

索引：`idx_op_log_target(module, target_id)`、`idx_op_log_operation(operation_id)`、`idx_op_log_module_created_at(module, created_at DESC)`

## pms_access_log 访问日志表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `user_id` | `BIGINT` | 引用 `pms_user.id` | 用户 |
| `employee_no` | `VARCHAR(50)` | 可空 | 工号 |
| `account` | `VARCHAR(100)` | 默认 `''` | 登录账号 |
| `real_name` | `VARCHAR(50)` | 可空 | 姓名 |
| `event_type` | `VARCHAR(30)` | 必填 | 事件类型 |
| `result` | `VARCHAR(30)` | 必填 | 访问结果 |
| `fail_reason` | `VARCHAR(100)` | 可空 | 失败原因 |
| `session_id` | `VARCHAR(100)` | 唯一 | 会话 ID |
| `login_at` | `TIMESTAMPTZ` | 可空 | 登录时间 |
| `logout_at` | `TIMESTAMPTZ` | 可空 | 退出时间 |
| `last_active_at` | `TIMESTAMPTZ` | 可空 | 最后活跃时间 |
| `duration_seconds` | `INTEGER` | 默认 `0` | 在线时长秒数 |
| `ip` | `VARCHAR(100)` | 可空 | IP 地址 |
| `user_agent` | `TEXT` | 可空 | 浏览器信息 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |

索引：`idx_access_log_created_at(created_at)`、`idx_access_log_session(session_id)`、`idx_access_log_employee_no(employee_no)`

## pms_mcp_client MCP 智能体凭据表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | MCP 客户端 ID |
| `name` | `VARCHAR(100)` | 必填 | 智能体名称 |
| `token_prefix` | `VARCHAR(20)` | 必填 | 凭据前缀，仅用于识别 |
| `token_hash` | `CHAR(64)` | 必填，唯一 | MCP 凭据 SHA-256 哈希，不保存明文 |
| `endpoint_type` | `VARCHAR(10)` | `query` / `action` | 允许访问的 MCP 入口 |
| `status` | `SMALLINT` | 默认 `1` | 启用状态 |
| `expires_at` | `TIMESTAMPTZ` | 可空 | 凭据过期时间 |
| `last_used_at` | `TIMESTAMPTZ` | 可空 | 最后使用时间 |
| `created_by` | `BIGINT` | 引用 `pms_user.id` | 签发人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 软删除标记 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 自动记录 | 创建和更新时间 |

索引：`token_hash` 唯一、`idx_mcp_client_active(endpoint_type, status, is_deleted, expires_at)`

## pms_mcp_audit_log MCP 调用审计表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 审计记录 ID |
| `request_id` | `UUID` | 必填，唯一 | MCP 请求标识 |
| `client_id` | `BIGINT` | 引用 `pms_mcp_client.id` | 调用智能体 |
| `user_id` | `BIGINT` | 引用 `pms_user.id` | 平台传入员工对应用户 |
| `employee_no` | `VARCHAR(50)` | 可空 | 调用时员工号快照 |
| `endpoint_type` | `VARCHAR(10)` | `query` / `action` | MCP 入口 |
| `protocol_method` | `VARCHAR(50)` | 必填 | MCP 协议方法 |
| `tool_name` | `VARCHAR(100)` | 可空 | 工具名称 |
| `risk_level` | `VARCHAR(10)` | 可空 | 风险等级 |
| `module` | `VARCHAR(50)` | 可空 | 业务模块 |
| `target_id` / `target_name` | `BIGINT` / `VARCHAR(200)` | 可空 | 业务对象 |
| `input_summary` | `JSONB` | 可空 | 脱敏后的输入摘要 |
| `result_status` | `VARCHAR(20)` | 必填 | 成功或失败 |
| `result_count` | `INTEGER` | 可空 | 返回记录数量 |
| `error_code` / `error_message` | `VARCHAR(50)` / `VARCHAR(200)` | 可空 | 错误摘要 |
| `duration_ms` | `INTEGER` | 默认 `0` | 执行耗时毫秒 |
| `ip` | `VARCHAR(100)` | 可空 | 来源 IP |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 调用时间 |

索引：请求号唯一；按客户端、用户、工具和时间建立查询索引。

## pms_mcp_action_ticket MCP 操作确认表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `UUID` | 主键 | 一次性确认号 |
| `client_id` | `BIGINT` | 必填，引用 MCP 客户端 | 调用智能体 |
| `user_id` | `BIGINT` | 必填，引用用户 | 当前员工 |
| `employee_no` | `VARCHAR(50)` | 必填 | 员工号快照 |
| `tool_name` | `VARCHAR(100)` | 必填 | 操作工具 |
| `arguments_hash` | `CHAR(64)` | 必填 | 业务参数哈希 |
| `preview` | `JSONB` | 必填 | 变更预览 |
| `idempotency_key` | `VARCHAR(100)` | 可空 | 防重复执行键 |
| `risk_level` | `VARCHAR(10)` | `low` / `medium` / `high` | 风险等级 |
| `status` | `VARCHAR(20)` | 默认 `pending` | `pending`、`executed`、`expired`、`failed` |
| `expires_at` | `TIMESTAMPTZ` | 必填 | 三十分钟确认有效期 |
| `executed_at` | `TIMESTAMPTZ` | 可空 | 执行时间 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |

索引：`idx_mcp_ticket_status_expires(status, expires_at)`；同一智能体、员工、工具和非空幂等键唯一。

## pms_user_preference 用户偏好设置表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `user_id` | `BIGINT` | 必填，引用 `pms_user.id`，唯一 | 用户 |
| `default_route` | `VARCHAR(200)` | 默认 `/home` | 默认进入页面 |
| `default_page_size` | `INTEGER` | 默认 `20` | 列表默认条数 |
| `appearance_mode` | `VARCHAR(20)` | 默认 `light` | 外观模式，当前仅支持浅色 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_user_preference_user(user_id)`

## pms_message 消息表

用途：后台通知和系统消息，记录接收人、类型、已读状态和跳转路径。

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 数据库主键 |
| `recipient_user_id` | `BIGINT` | 必填，引用 `pms_user.id` | 接收用户 |
| `type` | `VARCHAR(30)` | 必填 | `notification` 通知，`system` 系统消息 |
| `title` | `VARCHAR(120)` | 必填 | 消息标题 |
| `description` | `TEXT` | 必填，最长 1000 字符 | 消息正文 |
| `link_path` | `VARCHAR(200)` | 可空 | 点击后的前端跳转路径 |
| `read_at` | `TIMESTAMPTZ` | 可空 | 已读时间，`NULL` 表示未读 |
| `creator_id` | `BIGINT` | 可空 | 创建人或触发人 |
| `is_deleted` | `SMALLINT` | 默认 `0` | 删除标记 |
| `created_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 更新时间 |

索引：`idx_message_recipient_read(recipient_user_id, read_at, is_deleted)`、`idx_message_created_at(created_at)`

## pms_scheduled_task_execution 定时任务执行表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 执行记录 ID |
| `task_code` | `VARCHAR(100)` | 必填 | 定时任务编码 |
| `target_type` | `VARCHAR(50)` | 必填 | 业务对象类型 |
| `target_id` | `BIGINT` | 必填 | 业务对象 ID |
| `execution_key` | `VARCHAR(150)` | 必填 | 业务触发节点唯一键 |
| `status` | `VARCHAR(20)` | `running` / `success` / `failed` | 执行状态 |
| `attempt_count` | `INTEGER` | 默认 `1`，大于 0 | 执行尝试次数 |
| `error_message` | `VARCHAR(500)` | 可空 | 最近一次失败原因 |
| `result_data` | `JSONB` | 可空 | 执行结果摘要 |
| `started_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 本次开始时间 |
| `finished_at` | `TIMESTAMPTZ` | 可空 | 本次结束时间 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

唯一约束：`UNIQUE(task_code, target_type, target_id, execution_key)`，用于阻止同一业务触发节点重复执行。索引：`idx_scheduled_task_execution_status(task_code, status, updated_at)`。

## pms_integration_config 外部接口配置表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 接口配置 ID |
| `code` | `VARCHAR(50)` | 必填，唯一 | 接口编码，i8 同步固定为 `i8_it_operations` |
| `name` | `VARCHAR(100)` | 必填 | 接口名称 |
| `adapter_code` | `VARCHAR(60)` | 必填 | 已审核的接口适配器编码 |
| `endpoint_url` | `VARCHAR(500)` | 必填 | 外部接口地址 |
| `request_method` | `VARCHAR(10)` | 默认 `POST` | 请求方法 |
| `enabled` | `SMALLINT` | 默认 `0` | 接口是否启用 |
| `auto_enabled` | `SMALLINT` | 默认 `0` | 是否自动执行 |
| `sync_interval_hours` | `INTEGER` | 默认 `3` | 自动同步间隔小时数 |
| `auto_start_at` | `TIMESTAMPTZ` | 可空 | 自动执行首次开始时间；未配置时不自动执行 |
| `initial_sync_date` | `DATE` | 默认 `2026-08-24` | 首次同步起始日期 |
| `config_json` | `JSONB` | 默认 `{}` | 适配器专属配置；i8 保存固定产品和兜底负责人 |
| `last_cursor_at` | `TIMESTAMPTZ` | 可空 | 最近成功获取外部数据后的同步游标 |
| `last_started_at` / `last_finished_at` | `TIMESTAMPTZ` | 可空 | 最近执行起止时间 |
| `last_status` | `VARCHAR(20)` | `idle` / `running` / `success` / `failed` | 最近执行状态 |
| `last_total_count` | `INTEGER` | 默认 `0` | 最近处理总数 |
| `last_success_count` | `INTEGER` | 默认 `0` | 最近成功或跳过数量 |
| `last_failure_count` | `INTEGER` | 默认 `0` | 最近失败数量 |
| `last_warning_count` | `INTEGER` | 默认 `0` | 最近提醒数量 |
| `last_error` | `VARCHAR(1000)` | 可空 | 最近批次错误 |
| `creator_id` / `updater_id` | `BIGINT` | 可空，引用 `pms_user.id` | 创建人和更新人 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

## pms_integration_sync_record 接口同步明细表

| 字段 | 类型 | 约束/默认值 | 说明 |
|---|---|---|---|
| `id` | `BIGSERIAL` | 主键 | 同步记录 ID |
| `integration_config_id` | `BIGINT` | 必填，引用 `pms_integration_config.id` | 接口配置 |
| `batch_execution_id` | `BIGINT` | 可空，引用 `pms_scheduled_task_execution.id` | 所属执行批次 |
| `source_key` | `VARCHAR(100)` | 必填 | 来源数据唯一标识 |
| `source_type` | `VARCHAR(50)` | 必填 | 来源数据类型 |
| `target_type` | `VARCHAR(30)` | `requirement` / `work_order` | PMIS 目标类型 |
| `target_id` | `BIGINT` | 可空 | PMIS 目标记录 ID |
| `source_updated_at` | `TIMESTAMPTZ` | 可空 | 来源数据更新时间 |
| `payload_hash` | `VARCHAR(64)` | 必填 | 外部数据摘要，用于识别未变化记录 |
| `payload_summary` | `JSONB` | 可空 | 最近外部数据，用于失败重试和审计 |
| `sync_status` | `VARCHAR(20)` | `success` / `failed` / `skipped` | 单条同步状态 |
| `warning_message` | `VARCHAR(1000)` | 可空 | 负责人兜底、优先级降级等提醒 |
| `error_message` | `VARCHAR(1000)` | 可空 | 单条失败原因 |
| `synced_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 本次同步时间 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 默认 `NOW()` | 创建和更新时间 |

每次尝试都新增记录，不覆盖历史失败；同一接口与来源标识的最近成功记录用于定位当前业务映射。索引：`idx_integration_sync_record_status(integration_config_id, sync_status, synced_at DESC)`、`idx_integration_sync_record_source(integration_config_id, source_key, synced_at DESC, id DESC)`、`idx_integration_sync_record_target(target_type, target_id)`。
